name: calculator
description: Kpk practice.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0-125.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  function_tree: ^0.8.12
  url_launcher: ^6.0.20
  provider: ^6.0.2
  shared_preferences: ^2.0.13

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true